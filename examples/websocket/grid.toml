# not entirely settled on this
# it's how i think the runtime manifest should look
name = "websocket"
lang = "rust"

[function]
name = "websocket"
no-url = false
out = ".out"

[dev.secrets]
path = ".env.dev"
allowed_ip = ["0.0.0.0"]

[prod.secrets]
path = ".env"
allowed_ip = ["0.0.0.0"]
